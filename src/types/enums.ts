export enum CurrencyEnum {
  EGP = "EGP",
  USD = "USD",
  SAR = "SAR",
}

export enum AccountType {
  CASH = "CASH",
  CREDIT = "CREDIT",
  DEBIT = "DEBIT",
}

export enum CategoryType {
  INCOME = "INCOME",
  EXPENSE = "EXPENSE",
}

export enum TransactionType {
  EXPENSE = "EXPENSE",
  INCOME = "INCOME",
  TRANSFER = "TRANSFER",
}
